#Var 4

class employee():
    def __init__(self, name, employee_id, position, salary, work_hours)
    super.classEmployee():
    def word_time()
    self.getname
    self.getid(employee_id)
    self.get_total_hours(for work_hours in range)

    class manager(self)
